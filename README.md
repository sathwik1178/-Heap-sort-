# -Heap-sort-
Heap sort is a comparison-based sorting algorithm that uses a Binary Heap data structure to organize elements. It is essentially an optimized version of selection sort that reduces the time to find the maximum element from  o(n) to o(logn)
